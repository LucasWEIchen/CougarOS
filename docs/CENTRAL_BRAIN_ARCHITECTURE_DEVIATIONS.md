# 中央大脑架构偏差登记表

版本：0.1
日期：2026-07-07

## 使用规则

本文件记录所有软件开发与架构图需求基线不一致的地方。偏差不等于错误，但必须有原因、影响、修正计划和状态。任何新增代码、接口、文档如果绕开架构图中的层级，都必须先更新本文件。

状态定义：
- Open：偏差存在，未修正。
- Accepted Temporary：阶段性接受，但已有回收计划。
- Resolved：已修正。

## 偏差列表

| ID | 偏差 | 涉及需求 | 当前原因 | 风险 | 修正计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| DEV-001 | 当前仍以 REST/HTTP 承载 mock 后端；Android Console 已改为绑定 `CentralBrainGatewayBinderService`，Linux IPC sample 和 Linux gRPC/RPC sample 已对 SOA 调用增加 Runtime & Governance precheck，但 Binder service、Linux IPC 与 Linux gRPC/RPC 允许路径仍以上游 REST gateway 承载语义层 | FW-U-001..008, FW-S-001..006, NV-P-005, XSC-006 | M0 为快速验证端到端链路；A4/A4.2 已新增 Android Binder service/client sample、Linux Unix socket IPC active sample 和 Linux gRPC/RPC JSON contract sample；Android Console 经 Binder client 调用 `getStateJson`、`planAgentTaskJson`、`executeAgentTaskJson`、`invokeSkillJson` 和 `queryMemoryJson`；`/soa/contracts` 已把 Service Contract/Schema 从 runtime catalog 暴露到 Android Binder/Linux IPC/gRPC；`/uib/actions/request` 已作为 Action 语义入口映射到 Android Binder/Linux IPC/gRPC contract；Linux IPC 与 gRPC/RPC `soa.service.invoke` 已可优先调用 shared governance daemon 执行 discovery/policy/lifecycle/QoS precheck，并在不可用时回退本地 precheck；Android system/privileged service 集成约束已文档化 | 如果误把 Binder/IPC/gRPC sample 当成量产 gateway，会低估独立系统服务、权限身份、多进程治理、native gateway、签名、SELinux 和真实 gRPC runtime 工作；Linux shared governance daemon 仍是单机 Unix socket 样例，不代表量产治理后端；`/soa/contracts` 是 no-dispatch metadata，不代表完整 IDL compiler 或 schema registry；Action mock 只做 Policy 检查，不代表真实车控执行 | 下一步把 Android Binder/Linux IPC/gRPC sample 的上游替换为独立 gateway/daemon；目标 AAOS 镜像明确后按 `CENTRAL_BRAIN_ANDROID_SYSTEM_SERVICE_INTEGRATION.md` 补 framework/priv-app/sepolicy 集成；真实车控必须接入 Vehicle Signal/ECU Adapter 和 Driver/HAL 后再开放 | Accepted Temporary |
| DEV-002 | 当前 `mock_npu_service.py` 仍同时承担 HTTP gateway、vehicle state、NPU runtime；Registry/Discovery/Policy/Lifecycle/Audit 已拆到 `runtime_governance.py`，但仍是同进程原型 | NV-F-008, NV-F-011, NV-G-001..007 | M0/A3 用单进程 mock 降低复杂度，同时先形成可验证治理边界 | Gateway、SOA runtime、Model Runtime Adapter 尚未成为独立进程/模块 | M2 继续拆成 gateway、SOA runtime、model runtime adapter；保留 `runtime_governance.py` 作为治理边界原型 | Accepted Temporary |
| DEV-003 | Android Console 已暴露 `planAgentTaskJson`、`executeAgentTaskJson`、`invokeSkillJson`、`queryMemoryJson` 调试路径，AI SDK 已增加 `/agent/execute`、`/skills`、`/skills/{skill_id}/invoke`、`/memory/query` contract mock，但没有真实多模态、模型路由、Skill sandbox、持久 Memory/Privacy 或 Agent runtime | APP-004, NV-F-001, NV-F-011, XSC-001, FW-U-006 | 本轮让 Android Console 可直接触发 execute/Skill/Memory contract mock；这些路径只完成 Policy/Safety State 检查、audit 可见性和 Android/Linux binding contract；实际步骤只返回 SOA/Tool/Action dispatch 边界，不执行真实 Skill、Memory 写入、Model Runtime Adapter 或 Driver/HAL | 集成方可能把 contract mock 当成真实 Agent runtime；执行能力、Skill 安全边界、Memory/Privacy、模型路由和尚未验证的真实 sandbox 仍有风险 | M2 增加真实 Agent runtime、Skill sandbox、Memory/Privacy、真实模型路由，并保持执行经 SOA/Tool/Action 与 Runtime & Governance；Console 后续只作为调试入口，不替代系统 SDK/API | Accepted Temporary |
| DEV-004 | 当前车辆信号已有 `/vehicle/signals` 只读 VSS-style catalog、`/vehicle/signals/activation` 读桥激活准入 contract 和 `/vehicle/signals/validation` 读桥校验证据 envelope；Android 已暴露 `getVehicleSignalsJson`/`getVehicleSignalActivationJson`/`getVehicleSignalValidationJson`，Linux 已暴露 `vehicle-signals`/`vehicle-signal-activation`/`vehicle-signal-validation`、`vehicle.signals.list`/`vehicle.signals.activation.get`/`vehicle.signals.validation.get`、`GetVehicleSignals`/`GetVehicleSignalActivation`/`GetVehicleSignalValidation` 可见路径，但没有落到真实 Vehicle/Body Signal、ECU Proxy、DBC/ARXML、VHAL/HAL、SocketCAN 或 vendor gateway | NV-F-003, NV-F-004, NV-F-005, KH-006 | 没有真实车身信号源和 DBC/ARXML；`/native/driver-gaps` 已把 Vehicle bus 记录为 DRV-GAP-002；本轮只补信号目录、读桥激活准入门禁、schema-source/owner/parity/DRV-GAP-002 evidence 校验 envelope 和 Android/Linux binding 可见性 | 信号语义仍无法验证量产适配；集成方可能误把只读 mock catalog、activation criteria 或 validation envelope 当成真实 VHAL/ECU proxy 数据面或 DBC/ARXML validator | M4 在用户提供车型信号目录、DBC/ARXML、VHAL contract、SocketCAN 或 vendor gateway 后建立真实 Signal Adapter read bridge；所有车控写操作仍必须经 Vehicle Signal Adapter、Policy/Safety State、SOA/Uni Info Bus 和 Driver/HAL；若目标环境缺少用户态桥接能力，再按 DRV-GAP-002 记录最小 Driver/HAL 新增开发量；validation envelope 在真实桥接前必须收齐 owner、ABI、parity 和 smoke evidence | Open |
| DEV-005 | 外置 PCIe NPU 目前仅通过 Python mock 和 hardware empty-interface registry 表达，没有 Driver、HAL、Safety Runtime、DMA/IOMMU、真实 Model Runtime Adapter | HW-002, KH-003, KH-006, KH-007, NV-F-011 | 缺少真实 NPU 卡型号和 vendor SDK；A6 已新增 `CENTRAL_BRAIN_NPU_RUNTIME_INTERFACE.md` 固定 runtime/Driver/HAL contract，并通过 `/native/driver-gaps` 暴露 DRV-GAP-001 触发条件；本轮 `/hardware/interfaces` 暴露 `npu-runtime` 空接口和 reserved methods，但 `hardware_accessed=false`，驱动层只在环境缺口处新增开发 | AI 基座接口风险最高；如果后续绕过 Model Runtime Adapter 直连 vendor SDK，会破坏治理链路；如果误把 `/hardware/interfaces` 当成 HAL，会高估原型成熟度 | 获取硬件、vendor SDK、driver ABI 后只补 NPU runtime adapter + Driver/HAL bridge 缺口；DEV-005 在真实接入前保持 Open | Open |
| DEV-006 | Runtime & Governance 已参与 `/soa/invoke` 的 registry/discovery/policy/lifecycle/QoS/audit precheck，并新增 `POST /governance/precheck` 只检查不调用契约、`GET /governance/backend-contract` 共享治理后端目标契约、`GET /governance/migration-check` 迁移 readiness 检查、`GET /governance/deployment-plan` 部署计划 contract 和 `CENTRAL_BRAIN_AUDIT_LOG` JSONL 审计持久化样例；Linux IPC/gRPC 对 `soa.service.invoke` 可通过 `central_brain_governance_client.py` 调用 shared governance daemon，且保留本地 precheck fallback；Linux IPC/gRPC 对 `governance.runtime.get`/`GetRuntimeGovernance` 和 `audit.recent.get`/`GetRecentAudit` 也优先复用 shared governance socket diagnostic path，不可用时回退 REST；QoS 当前仍是单进程 fixed-window active prototype | NV-G-001, NV-G-002, NV-G-004, NV-G-005, NV-G-006, NV-G-007, XSC-005, DEL-001, DEL-002, DEL-003, DEL-004 | A3 先把治理从状态展示推进到 active prototype；本轮把 precheck、backend contract、migration check 和 deployment plan 显式成 Android/Linux binding contract，并让 `central_brain_governance_daemon.py` 作为 Linux 共享治理 socket 样例提供 precheck、runtime status 和 audit diagnostics，使 IPC/gRPC binding 通过同一 client helper 复用同一进程内决策而不 dispatch 服务 | 治理能力仍不能约束真实多机/多协议服务；JSONL 只适合开发/单机集成，不等于量产审计后端；后端 gateway 与 governance daemon 仍各自持有 QoS 窗口；`/governance/backend-contract` 只是替换规则和目标操作清单；`/governance/migration-check` 与 `/governance/deployment-plan` 明确 `production_backend_ready=false`；shared governance client 只收敛 socket envelope 和 Linux 诊断路径，不代表 `/governance/precheck` 或 Linux governance daemon 已成为量产治理后端 | M2 增加跨进程/跨协议 QoS、真实 registry/discovery、审计轮转/导出，并让 IPC/gRPC/Binder binding 共用同一治理服务或 daemon；Linux governance daemon/client/backend contract/migration check/deployment plan 将作为共享治理 daemon 的候选接口继续演进 | Accepted Temporary |
| DEV-007 | 当前已有 `/uib/events/topics`、`/uib/events/publish`、`/uib/events/recent` Event active mock、`/uib/events/subscriptions` 订阅契约、`POST /uib/events/subscriptions/request` 与 `POST /uib/events/subscriptions/cancel` 生命周期命令契约、`GET /uib/events/subscriptions/transport-readiness` callback/watch transport readiness 契约、`GET /uib/events/subscriptions/decision-matrix` broker/cursor/backpressure owner decision matrix、`GET /uib/events/subscriptions/activation-checklist` broker activation 前置证据清单、`GET /uib/events/subscriptions/callback-watch-shape` Android callback/Linux watch API shape 契约，以及 Android Binder/Linux IPC/gRPC 映射，但仍没有真实订阅 broker、callback/watch、SSE/WebSocket/DDS 推送或高频 topic 数据面 | FW-U-003, NV-P-006, XSC-002, XSC-006 | 本轮在 Uni Info Bus Event 语义 contract、bounded recent log、订阅 lifecycle/cursor/backpressure/governance 契约、request/cancel contract-only 命令、transport readiness contract、owner decision matrix 和 activation checklist 基础上补齐 callback/watch API shape，返回 Android planned callback、Linux planned watch lifecycle、event/overflow/close envelope、Runtime & Governance binding、cursor/reconnect、no-runtime-registration `EV-CW-001..008` 门禁和 `shape_confirmed=false` 边界，避免把 API shape 误当作真实 callback/watch runtime | 车辆信号、服务健康、AI 任务仍无法以量产实时流方式推送；高频数据背压、QoS、订阅持久化、回放、取消语义、activation evidence 和 callback/watch 尚未在真实 broker 上验证 | M2 收齐 broker owner、cursor storage、callback/watch 形态、持久化 owner、transport 选择、QoS/backpressure owner 和 activation evidence；M3 才能选择 SSE/WebSocket/mock DDS；目标高频环境明确后接 DDS binding | Accepted Temporary |
| DEV-008 | 当前没有 Hypervisor、ASIL/QM 隔离、跨 VM 通信实现 | HV-001..003 | 用户已明确虚拟化层不需要开发；A7 已新增 `CENTRAL_BRAIN_VIRTUALIZATION_SAFETY_CONSTRAINTS.md` 固定接口约束 | 如果误纳入开发会浪费范围；如果下游忽略约束会影响 Safety/跨域集成 | 继续只维护安全域映射、依赖假设、跨 VM envelope 和 fallback；目标 SoC/Hypervisor 明确后进入集成验证，不开发虚拟化功能 | Accepted Temporary |
| DEV-009 | KaKaClaw/咖咖虾相关 Agentic OS 概念是产品参考，不属于原图直接模块；若直接扩展会偏离图中基线 | APP-004, NV-F-001, FW-U-006 | 用户要求参考该产品 | 产品概念覆盖架构图需求 | 所有 Agent/Skill/Memory 功能必须映射到 AI SDK、AIOS Kernel、Tool、Policy，不单独替代基线 | Accepted Temporary |
| DEV-010 | 当前路线图先写了 M0/M0.1，但没有明确“架构图需求基线优先级高于产品参考” | 全部 | 初期把图当概念参考 | 开发计划优先级错误 | 本轮修订路线图和跟踪规则 | Resolved |
| DEV-011 | Linux 已有 CLI、Unix socket IPC daemon/client、gRPC/RPC JSON contract sample、systemd 部署样例、systemd hardening check、package profile check 和平台差异说明，但尚未提供量产包管理、真实 IPC 服务治理和驱动/HAL 集成 | DEL-002, DEL-003, DEL-004, XSC-001..006, NV-P-002, NV-P-003 | 本轮补最小 Linux 同步交付路径，并为 gateway、governance、IPC、gRPC/RPC unit 增加 `ProtectSystem=strict`、`ProtectHome=true`、`PrivateDevices=true`、`NoNewPrivileges=true`、`ReadWritePaths` 等样例约束；`central-brain.package-profile.json` 进一步固定 install root、service identity、env file、runtime/log 目录、Req ID 和非目标边界 | Linux 座舱工程师仍需按目标发行版补包管理、安全加固基线评审、LSM/SELinux/AppArmor 策略、真实 Driver/HAL 和进程权限集成；当前 hardening 与 package profile 仍是样例，不代表量产安全认证或发行版包 | A9 继续补目标发行版 packaging/profile、真实 IPC/gRPC binding 与平台差异细节；A6 只在硬件/SDK 明确后补 Driver/HAL 缺口 | Accepted Temporary |
| DEV-012 | 当前未显式区分黄色小太阳跨 SoC 组件和普通应用/生态组件 | XSC-001..006 | 初版只按层级拆解 | 跨 SoC 复用组件可能被做成单平台实现 | 已新增 XSC-001..006；后续所有 XSC 组件必须同时规划 Android/Linux 和平台无关 contract | Resolved |
| DEV-013 | A4 Protocol Binding 目前 Android Console 已绑定 Binder service stub sample，且可直接触发 `precheckGovernanceJson` 只检查不调用路径；Android Binder 也暴露 `getGovernanceBackendContractJson`、`getGovernanceMigrationCheckJson`、`getGovernanceDeploymentPlanJson`、`getBindingReadinessJson` 和 `getDeliveryReadinessJson`；Linux Unix socket IPC 是 active sample 且对 SOA 调用可走 shared governance daemon precheck，并可通过 `governance.backend.contract.get`/`governance.migration.check`/`governance.deployment.plan.get`/`bindings.readiness.get`/`delivery.readiness.get` 查询目标共享治理后端契约、迁移 readiness、部署计划、Protocol Binding readiness 和 Android/Linux delivery readiness；Linux IPC 的 `governance.runtime.get`/`audit.recent.get` 和 Linux gRPC/RPC 的 `GetRuntimeGovernance`/`GetRecentAudit` 现在优先通过同一 `central_brain_governance_client.py` 直连 shared governance socket，失败时回退 REST gateway；Linux gRPC/RPC 已有 JSON contract sample 验证 proto envelope、`InvokeService` shared governance precheck、`GetGovernanceBackendContract`、`GetGovernanceMigrationCheck`、`GetGovernanceDeploymentPlan`、`GetBindingReadiness` 和 `GetDeliveryReadiness` 映射；Android Binder/Linux IPC/gRPC contract 已映射 `governance.precheck`/`precheckGovernanceJson`/`PrecheckGovernance`；Android Binder、Linux IPC 和 Linux gRPC/RPC sample 允许路径仍代理到同进程 REST gateway，Event 也只是语义 mock/recent log；Android system/privileged service 仍是文档化集成约束 | XSC-006, NV-P-001, NV-P-002, NV-P-003, NV-P-004, NV-P-005, NV-P-006, DEL-001, DEL-002, DEL-003, DEL-004, XSC-005 | 当前增量先固化跨 SoC 绑定 contract 和语义入口映射，并让 Android debug APK 实际走 Binder client/service 路径；Console `Precheck` 按钮、backend contract、migration check、deployment plan 和 binding readiness 只暴露 Runtime & Governance diagnostic/target/readiness/deployment、Protocol Binding readiness 与 Android/Linux delivery readiness contract，不代表量产共享治理后端或量产 transport；Linux governance daemon socket 让 Linux IPC 和 gRPC/RPC sample 通过同一 client helper 复用共享 precheck 进程，并把 runtime/audit 只读诊断也收敛到同一 shared socket；当前环境没有 `grpcio`，因此 gRPC/RPC sample 使用 JSON TCP wrapper 验证 contract | 集成工程师可能误以为 Binder/IPC/gRPC sample 已是完整系统服务、多进程治理运行时、真实 gRPC server、MQTT/SOME-IP/DDS 数据面、DDS 事件总线或目标镜像 sepolicy/签名配置；Linux governance daemon/client、`/governance/precheck`、`/governance/backend-contract`、`/governance/migration-check`、`/governance/deployment-plan`、`/bindings/readiness`、`/delivery/readiness` 与后端 gateway precheck 仍不是共享量产治理服务或量产 transport；runtime/audit direct diagnostics 仍是 Linux 单机样例 | `/bindings/detail` 明确 `android-binder-aidl` 为 `service-stub-sample`、`linux-ipc` 为 `active-sample with shared SOA governance precheck/runtime/audit diagnostics/backend contract/migration/deployment visibility`、gRPC 为 `grpc-json-active-sample`、DDS 为计划态；`/bindings/readiness` 明确 Android/Linux binding readiness、阻塞项、验证命令和 `production_ready=false`；`/delivery/readiness` 明确 Android/Linux delivery readiness、验证 bundle、阻塞项和 `production_ready=false`；`CENTRAL_BRAIN_ANDROID_SYSTEM_SERVICE_INTEGRATION.md` 明确 system/privileged service 仍需目标 AAOS 镜像决策；下一步在目标环境补真实 gRPC runtime、量产共享治理 daemon、Linux 独立 gateway/daemon 或 Android 目标镜像适配 | Accepted Temporary |
| DEV-014 | A5 Native adapters 目前是注册表和边界 mock，不是真实 AIOS Kernel、VHAL/ECU adapter、Model Runtime daemon 或 Driver/HAL bridge | XSC-004, NV-F-001, NV-F-003, NV-F-004, NV-F-005, NV-F-008, NV-F-011, KH-003, KH-006, DEL-005 | 当前 Android/Linux 环境没有真实车身信号源、DBC/ARXML、NPU vendor SDK 或 Driver/HAL；用户要求驱动层仅在能力不足时新增最小开发量；Console `Driver Gaps` 入口、`/native/driver-gaps`、Android `getDriverHalGapsJson` 和 Linux CLI `driver-gaps` 只是 backlog contract；本轮 `/hardware/interfaces`、Android `getHardwareInterfacesJson`、Linux `hardware-interfaces` 只是空接口目录 | 集成方可能误以为 Native adapter、Driver/HAL gap backlog 或 hardware empty-interface registry 已具备量产数据面能力 | `/native/adapters/detail`、`/native/driver-gaps` 和 `/hardware/interfaces` 明确状态、Android/Linux 交付路径、触发条件、reserved methods 和 Driver/HAL 依赖；后续按真实信号目录、vendor SDK 或 IPC daemon 逐项替换 mock | Accepted Temporary |
| DEV-015 | FW-U-008 扩展机制目前是 `/uib/extensions` 只读 registry contract，不是动态插件 runtime、extension loader 或 schema registry 服务 | XSC-002, FW-U-008, XSC-005, XSC-006, NV-P-002, NV-P-003, DEL-001, DEL-002 | 当前架构图只写“其他/扩展语义”，尚未给出动态扩展生命周期、schema 发布、权限审核或插件沙箱要求；本轮先固定 extension object 必须保留 Uni Info Bus envelope、Runtime & Governance、Protocol Binding 和 no-dispatch 边界 | 集成方可能把 extension registry contract 误认为已经能加载插件或新增数据面；如果扩展绕过 Policy/Audit，会破坏核心语义对象一致性 | `/uib/extensions`、Android `getUibExtensionsJson`、Linux `extensions`/`uib.extensions.get`/`GetUibExtensions` 明确 `dynamic_extension_runtime_ready=false`、`service_dispatch_triggered=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false`；目标 extension lifecycle/schema registry 明确后再进入动态 runtime | Accepted Temporary |
| DEV-016 | A6/A6.1 hardware empty-interface registry、activation checklist、owner decision status、owner decision evidence intake、owner decision evidence status、owner decision evidence retention/closure checklist 和 owner decision evidence replacement trigger checklist 只是 Python 原型中的硬件依赖接口目录、激活前门禁、未决 owner 状态汇总、no-store evidence reference 入站形状、no-store status rollup、retention/closure 决策清单与空接口替换触发决策清单，不是 Driver/HAL、vendor SDK、Safety Runtime、共享内存、车辆总线、证据库、review workflow、delete/export workflow、gate closure workflow、adapter replacement workflow 或虚拟化实现 | XSC-004, XSC-006, HW-002, KH-003, KH-006, KH-007, DEL-001, DEL-002, DEL-005 | 用户要求真实硬件依赖全部预留空接口；已新增 `central-brain/backend/hardware_interfaces.py`、`GET /hardware/interfaces`、Android `getHardwareInterfacesJson`、Linux `hardware-interfaces`/`hardware.interfaces.get`/`GetHardwareInterfaces`，已新增 `GET /hardware/interfaces/activation-checklist`、Android `getHardwareInterfaceActivationChecklistJson`、Linux `hardware-interface-activation-checklist`/`hardware.interfaces.activation.checklist`/`GetHardwareInterfaceActivationChecklist`，已新增 `GET /hardware/interfaces/owner-decision-status`、Android `getHardwareInterfaceOwnerDecisionStatusJson`、Linux `hardware-interface-owner-decision-status`/`hardware.interfaces.owner.decision.status`/`GetHardwareInterfaceOwnerDecisionStatus`，已新增 `POST /hardware/interfaces/owner-decision-evidence`、Android `submitHardwareInterfaceOwnerDecisionEvidenceJson`、Linux `hardware-interface-owner-decision-evidence`/`hardware.interfaces.owner.decision.evidence`/`SubmitHardwareInterfaceOwnerDecisionEvidence`，已新增 `GET /hardware/interfaces/owner-decision-evidence/status`、Android `getHardwareInterfaceOwnerDecisionEvidenceStatusJson`、Linux `hardware-interface-owner-decision-evidence-status`/`hardware.interfaces.owner.decision.evidence.status`/`GetHardwareInterfaceOwnerDecisionEvidenceStatus`，已新增 `GET /hardware/interfaces/owner-decision-evidence/retention-checklist`、Android `getHardwareInterfaceOwnerDecisionEvidenceRetentionChecklistJson`、Linux `hardware-interface-owner-decision-evidence-retention-checklist`/`hardware.interfaces.owner.decision.evidence.retention.checklist`/`GetHardwareInterfaceOwnerDecisionEvidenceRetentionChecklist`，本轮新增 `GET /hardware/interfaces/owner-decision-evidence/replacement-trigger-checklist`、Android `getHardwareInterfaceOwnerDecisionEvidenceReplacementTriggerChecklistJson`、Linux `hardware-interface-owner-decision-evidence-replacement-trigger-checklist`/`hardware.interfaces.owner.decision.evidence.replacement.trigger.checklist`/`GetHardwareInterfaceOwnerDecisionEvidenceReplacementTriggerChecklist`，并固定 `activation_allowed=false`、`replacement_allowed=false`、`adapter_activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false`、`virtualization_development_triggered=false`、`owner_decision_evidence_persisted=false`、`evidence_store_active=false`、`review_workflow_active=false`、`delete_workflow_active=false`、`export_workflow_active=false`、`gate_closure_allowed=false` 和 `review_queue_updated=false` | 如果交付对象把空接口目录、activation checklist、owner decision status、evidence intake、evidence status、retention/closure checklist 或 replacement trigger checklist 当成已验证硬件抽象层、证据存储、review workflow、delete/export workflow、gate closure workflow 或 adapter replacement workflow，会低估 PCIe NPU、VHAL/车身总线、Camera/Audio/Sensor、SOME/IP/DDS/TSN、shared-memory/Safety Runtime 的真实集成量 | 在真实硬件、vendor SDK、信号目录、网络栈、Safety Runtime、owner、ABI、evidence store owner、review workflow、gate closure authority、target hardware smoke evidence、delete/export semantics、approval signature、rollback/fault 语义、adapter readiness criteria 和 Driver/HAL gap closure evidence 明确前，只维护 contract、gap backlog、activation gates、owner decision status、no-store evidence intake/status/retention-closure/replacement-trigger checklist 和 smoke 断言；后续每个空接口替换为真实 adapter 时必须新增偏差追踪和验证 | Accepted Temporary |

## DEV-007 增量补充

2026-07-07 新增 `GET /uib/events/subscriptions/cursor-replay-storage`、Android Binder `getEventSubscriptionCursorReplayStorageJson`、Linux CLI `event-subscription-cursor-replay-storage`、Linux IPC `uib.events.subscriptions.cursor.replay.storage` 和 Linux gRPC/RPC `GetEventSubscriptionCursorReplayStorage`，补齐 cursor schema、ack shape、replay window、retention/cleanup 与 `EV-CRS-001..008` contract。该补充没有关闭 DEV-007：`cursor_replay_storage_confirmed=false`、`subscription_persistence_active=false`、`cursor_storage_active=false`、`replay_index_active=false`、`broker_active=false`、`callback_registered=false`、`watch_started=false` 仍是验收边界。

2026-07-07 新增 `GET /uib/events/subscriptions/backpressure-qos-evidence`、Android Binder `getEventSubscriptionBackpressureQosEvidenceJson`、Linux CLI `event-subscription-backpressure-qos-evidence`、Linux IPC `uib.events.subscriptions.backpressure.qos.evidence` 和 Linux gRPC/RPC `GetEventSubscriptionBackpressureQosEvidence`，补齐 overflow schema、per-caller throttling、replay rate、ack timeout、Runtime & Governance QoS evidence 和 `EV-QOS-001..008` contract。该补充没有关闭 DEV-007：`backpressure_qos_evidence_confirmed=false`、`event_delivery_qos_active=false`、`overflow_emission_active=false`、`broker_active=false`、`dds_runtime_active=false`、`high_rate_data_plane_active=false`、`driver_development_triggered=false` 仍是验收边界。

2026-07-07 新增 `GET /uib/events/subscriptions/readiness-rollup`、Android Binder `getEventSubscriptionReadinessRollupJson`、Linux CLI `event-subscription-readiness-rollup`、Linux IPC `uib.events.subscriptions.readiness.rollup` 和 Linux gRPC/RPC `GetEventSubscriptionReadinessRollup`，把 lifecycle、transport readiness、owner decision matrix、activation checklist、callback/watch shape、cursor/replay storage、backpressure/QoS evidence 的 blocked gates 汇总为 `EV-RU-001..006` activation blockers。该补充没有关闭 DEV-007：`readiness_rollup_confirmed=false`、`broker_activation_ready=false`、`production_activation_allowed=false`、`all_required_evidence_complete=false`、`transport_selected=false`、`broker_active=false`、`cursor_storage_active=false`、`event_delivery_qos_active=false`、`callback_registered=false`、`watch_started=false`、`dds_runtime_active=false`、`driver_development_triggered=false` 仍是验收边界。

2026-07-07 新增 `POST /uib/events/subscriptions/activation-evidence`、Android Binder `submitEventSubscriptionActivationEvidenceJson`、Linux CLI `event-subscription-activation-evidence`、Linux IPC `uib.events.subscriptions.activation.evidence` 和 Linux gRPC/RPC `SubmitEventSubscriptionActivationEvidence`，把 activation gate evidence reference envelope、reviewer identity、policy/audit check 和 `EV-AE-001..008` 门禁固定为 contract-only intake。该补充没有关闭 DEV-007：`activation_evidence_persisted=false`、`activation_evidence_accepted_for_review=false`、`review_queue_updated=false`、`gate_state_changed=false`、`gates_closed=false`、`activation_allowed=false`、`broker_activation_ready=false`、`broker_active=false`、`driver_development_triggered=false` 仍是验收边界。

2026-07-07 新增 `GET /uib/events/subscriptions/activation-evidence/status`、Android Binder `getEventSubscriptionActivationEvidenceStatusJson`、Linux CLI `event-subscription-activation-evidence-status`、Linux IPC `uib.events.subscriptions.activation.evidence.status` 和 Linux gRPC/RPC `GetEventSubscriptionActivationEvidenceStatus`，把 activation evidence intake 之后的只读 review status 固定为 contract-only no-store/no-workflow 状态。该补充没有关闭 DEV-007：`activation_evidence_status_contract_active=true` 只表示状态接口可查，`evidence_store_active=false`、`review_workflow_active=false`、`persisted_submission_count=0`、`pending_review_count=0`、`review_queue_updated=false`、`gates_closed=false`、`activation_allowed=false`、`broker_active=false`、`driver_development_triggered=false` 仍是验收边界。

2026-07-07 新增 `GET /uib/events/subscriptions/activation-evidence/retention-checklist`、Android Binder `getEventSubscriptionActivationEvidenceRetentionChecklistJson`、Linux CLI `event-subscription-activation-evidence-retention-checklist`、Linux IPC `uib.events.subscriptions.activation.evidence.retention.checklist` 和 Linux gRPC/RPC `GetEventSubscriptionActivationEvidenceRetentionChecklist`，把 activation evidence durable store、URI rules、retention policy、review workflow owner、gate closure authority、delete/export semantics 固定为 contract-only 决策清单。该补充没有关闭 DEV-007：`activation_evidence_retention_checklist_active=true` 只表示清单可查，`owner_decision_complete=false`、`retention_policy_confirmed=false`、`evidence_uri_rules_confirmed=false`、`review_workflow_owner_confirmed=false`、`gate_closure_authority_confirmed=false`、`deletion_export_semantics_confirmed=false`、`evidence_store_active=false`、`review_workflow_active=false`、`delete_workflow_active=false`、`export_workflow_active=false`、`gates_closed=false`、`activation_allowed=false`、`broker_active=false`、`driver_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /uib/events/subscriptions/activation-evidence/decision-status-rollup`、Android Binder `getEventSubscriptionActivationEvidenceDecisionStatusRollupJson`、Linux CLI `event-subscription-activation-evidence-decision-status-rollup`、Linux IPC `uib.events.subscriptions.activation.evidence.decision.status.rollup` 和 Linux gRPC/RPC `GetEventSubscriptionActivationEvidenceDecisionStatusRollup`，把 activation evidence intake/status/retention checklist 与 readiness rollup 汇总为 `EV-AED-001..008` contract-only 决策状态。该补充没有关闭 DEV-007：`activation_evidence_decision_status_rollup_active=true` 只表示汇总可查，`decision_status_consistent=true` 只说明现有 contract surface 一致，`decision_status_passed=false`、`owner_decision_complete=false`、`activation_evidence_intake_called=false`、`activation_evidence_persisted=false`、`review_queue_updated=false`、`gates_closed=false`、`activation_allowed=false`、`broker_active=false`、`driver_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-dry-run/status`、Android Binder `getEventSubscriptionActivationApprovalDryRunStatusJson`、Linux CLI `event-subscription-activation-approval-dry-run-status`、Linux IPC `uib.events.subscriptions.activation.approval.dry.run.status` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDryRunStatus`，把 activation approval dry-run 的 last-result/no-store status 固定为 `EV-AAS-001..008` contract-only 状态。该补充没有关闭 DEV-007：`activation_approval_dry_run_status_active=true` 只表示状态可查，`decision_status_passed=false`、`owner_decision_complete=false`、`approval_dry_run_invoked=false`、`last_result_available=false`、`persisted_dry_run_count=0`、`approval_authority_assigned=false`、`approval_policy_confirmed=false`、`approval_result_store_active=false`、`review_queue_updated=false`、`gates_closed=false`、`activation_allowed=false`、`broker_active=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist`、Android Binder `getEventSubscriptionActivationApprovalAuthorityChecklistJson`、Linux CLI `event-subscription-activation-approval-authority-checklist`、Linux IPC `uib.events.subscriptions.activation.approval.authority.checklist` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalAuthorityChecklist`，把 activation approval authority、approval policy、signature/RBAC、result store、review queue、gate closure authority、broker activation owner 和 DRV-GAP-004/005 owner 固定为 `EV-AAA-001..008` contract-only 清单。该补充没有关闭 DEV-007：`activation_approval_authority_checklist_active=true` 只表示清单可查，`approval_authority_ready=false`、`approval_authority_assigned=false`、`approval_policy_confirmed=false`、`approval_signature_rbac_confirmed=false`、`approval_result_store_active=false`、`review_queue_owner_assigned=false`、`gate_closure_authority_assigned=false`、`broker_activation_owner_assigned=false`、`approval_dry_run_invoked=false`、`review_queue_updated=false`、`gates_closed=false`、`activation_allowed=false`、`broker_active=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/audit-consistency`、Android Binder `getEventSubscriptionActivationApprovalAuthorityAuditConsistencyJson`、Linux CLI `event-subscription-activation-approval-authority-audit-consistency`、Linux IPC `uib.events.subscriptions.activation.approval.authority.audit.consistency` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalAuthorityAuditConsistency`，把 approval authority checklist、approval dry-run status、activation evidence decision status rollup、authority item/blocker counters、Android/Linux parity 和 no-store/no-side-effect 约束固定为 `EV-AAC-001..008` contract-only 审计一致性。该补充没有关闭 DEV-007：`activation_approval_authority_audit_consistency_active=true` 和 `consistency_passed=true` 只说明现有 contract surfaces 互相一致，`approval_authority_ready=false`、`approval_authority_assigned=false`、`approval_policy_confirmed=false`、`approval_result_store_active=false`、`review_queue_updated=false`、`gates_closed=false`、`activation_allowed=false`、`broker_active=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

## DEV-016 增量补充

2026-07-07 新增 `POST /hardware/interfaces/owner-decision-evidence`、Android Binder `submitHardwareInterfaceOwnerDecisionEvidenceJson`、Linux CLI `hardware-interface-owner-decision-evidence`、Linux IPC `hardware.interfaces.owner.decision.evidence` 和 Linux gRPC/RPC `SubmitHardwareInterfaceOwnerDecisionEvidence`，把 hardware owner/smoke/ABI/Driver-HAL/Safety gate evidence reference envelope、reviewer identity、policy/audit check 和 `HW-ODE-001..008` 门禁固定为 contract-only intake。该补充没有关闭 DEV-016：`owner_decision_evidence_persisted=false`、`review_queue_updated=false`、`owner_assigned=false`、`gate_state_changed=false`、`gates_closed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-07 新增 `GET /hardware/interfaces/owner-decision-evidence/status`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceStatusJson`、Linux CLI `hardware-interface-owner-decision-evidence-status`、Linux IPC `hardware.interfaces.owner.decision.evidence.status` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceStatus`，把 hardware owner evidence intake 之后的 no-store/no-review status 固定为 contract-only rollup。该补充没有关闭 DEV-016：`evidence_store_active=false`、`review_workflow_active=false`、`persisted_submission_count=0`、`pending_review_count=0`、`owner_assigned=false`、`gate_state_changed=false`、`gates_closed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-07 新增 `GET /hardware/interfaces/owner-decision-evidence/retention-checklist`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceRetentionChecklistJson`、Linux CLI `hardware-interface-owner-decision-evidence-retention-checklist`、Linux IPC `hardware.interfaces.owner.decision.evidence.retention.checklist` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceRetentionChecklist`，把 hardware owner evidence durable store、URI rules、retention policy、review workflow、gate closure authority、delete/export semantics 和 rollback/fault closure evidence 固定为 contract-only 决策清单。该补充没有关闭 DEV-016：`owner_decision_evidence_retention_checklist_active=true` 只表示清单可查，`evidence_store_active=false`、`review_workflow_active=false`、`delete_workflow_active=false`、`export_workflow_active=false`、`owner_assigned=false`、`gate_state_changed=false`、`gates_closed=false`、`gate_closure_allowed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/replacement-trigger-checklist`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceReplacementTriggerChecklistJson`、Linux CLI `hardware-interface-owner-decision-evidence-replacement-trigger-checklist`、Linux IPC `hardware.interfaces.owner.decision.evidence.replacement.trigger.checklist` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceReplacementTriggerChecklist`，把 hardware empty-interface replacement target、adapter readiness criteria、Driver/HAL gap closure evidence、Android/Linux ABI replacement parity、rollback-to-empty-interface plan、Safety/Policy replacement review 和 smoke harness replacement evidence 固定为 contract-only 决策清单。该补充没有关闭 DEV-016：`owner_decision_evidence_replacement_trigger_checklist_active=true` 只表示清单可查，`replacement_allowed=false`、`adapter_activation_allowed=false`、`gate_closure_allowed=false`、`evidence_store_active=false`、`review_workflow_active=false`、`owner_assigned=false`、`gates_closed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/selected-adapter-readiness-checklist`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceSelectedAdapterReadinessChecklistJson`、Linux CLI `hardware-interface-owner-decision-evidence-selected-adapter-readiness-checklist`、Linux IPC `hardware.interfaces.owner.decision.evidence.selected.adapter.readiness.checklist` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceSelectedAdapterReadinessChecklist`，把 selected adapter owner、adapter interface contract、Driver/HAL gap evidence、Android/Linux binding parity、Safety/Policy fault model、smoke harness plan、rollback-to-empty-interface review 和 no-adapter-load parity 固定为 `HW-OEA-001..008` contract-only 决策清单。该补充没有关闭 DEV-016：`owner_decision_evidence_selected_adapter_readiness_checklist_active=true` 只表示清单可查，`adapter_candidate_recorded=false`、`adapter_load_allowed=false`、`adapter_activation_allowed=false`、`hardware_access_allowed=false`、`gate_closure_allowed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-blocker-rollup`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadBlockerRollupJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-blocker-rollup`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.blocker.rollup` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadBlockerRollup`，把 activation checklist、owner decision status、owner evidence status、retention checklist、replacement trigger checklist 和 selected-adapter readiness checklist 的未清项聚合为 `HW-ALB-001..008` contract-only 阻塞汇总。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_blocker_rollup_active=true` 只表示汇总可查，`adapter_load_ready=false`、`all_blockers_cleared=false`、`adapter_load_allowed=false`、`adapter_activation_allowed=false`、`hardware_access_allowed=false`、`gate_closure_allowed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `POST /hardware/interfaces/owner-decision-evidence/adapter-load-dry-run`、Android Binder `dryRunHardwareInterfaceOwnerDecisionEvidenceAdapterLoadJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-dry-run`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.dry.run` 和 Linux gRPC/RPC `DryRunHardwareInterfaceOwnerDecisionEvidenceAdapterLoad`，把 adapter-load approval request 固定为 contract-only dry-run，并强制引用 adapter-load blocker rollup 的开放阻塞项。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_dry_run_active=true` 只表示 dry-run 可查，`adapter_load_dry_run_state=rejected_blocked_contract_only`、`adapter_load_allowed=false`、`adapter_activation_allowed=false`、`hardware_access_allowed=false`、`gate_closure_allowed=false`、`review_queue_updated=false`、`evidence_persisted=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-dry-run/status`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadDryRunStatusJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-dry-run-status`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.dry.run.status` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadDryRunStatus`，把 adapter-load dry-run 后的 last-result/status 固定为 no-store 只读视图。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_dry_run_status_active=true` 只表示 status 可查，`last_result_available=false`、`persisted_dry_run_count=0`、`pending_review_count=0`、`review_queue_updated=false`、`evidence_persisted=false`、`adapter_load_allowed=false`、`hardware_access_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-dry-run/audit-consistency`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadDryRunAuditConsistencyJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-dry-run-audit-consistency`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.dry.run.audit.consistency` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadDryRunAuditConsistency`，把 adapter-load dry-run、dry-run/status、blocker rollup 和 gate family 的一致性固定为 no-store 只读视图。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_dry_run_audit_consistency_active=true` 只表示审计一致性可查，`consistency_passed=true` 只说明 contract surfaces 互相一致，`adapter_load_allowed=false`、`hardware_access_allowed=false`、`review_queue_updated=false`、`evidence_store_active=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalAuthorityChecklistJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-authority-checklist`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.authority.checklist` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalAuthorityChecklist`，把 adapter-load 从 dry-run 进入真实 approval/load 前的 authority、policy、signature/RBAC、durable evidence workflow、target smoke/rollback/fault evidence 固定为 `HW-ALA-001..008` contract-only 决策清单。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_approval_authority_checklist_active=true` 只表示清单可查，`approval_authority_assigned=false`、`approval_policy_confirmed=false`、`approval_signature_rules_confirmed=false`、`approval_rbac_confirmed=false`、`approval_workflow_active=false`、`approval_record_persisted=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/status`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalAuthorityStatusJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-authority-status`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.authority.status` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalAuthorityStatus`，把 adapter-load approval authority checklist 之后的 approval record/review/evidence no-store 状态固定为 `HW-AAS-001..008` contract-only 只读视图。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_approval_authority_status_active=true` 只表示状态可查，`approval_record_available=false`、`persisted_approval_record_count=0`、`pending_approval_review_count=0`、`approval_review_queue_updated=false`、`approval_evidence_store_active=false`、`approval_decision_passed=false`、`approval_decisions_open=true`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/audit-consistency`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalAuthorityAuditConsistencyJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-authority-audit-consistency`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.authority.audit.consistency` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalAuthorityAuditConsistency`，把 approval authority checklist、approval no-store status、dry-run audit consistency 和 blocker rollup 的一致性固定为 `HW-AAC-001..008` contract-only 只读视图。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_approval_authority_audit_consistency_active=true` 只表示审计一致性可查，`consistency_passed=true` 只说明现有 contract surfaces 互相一致，`approval_status_no_store_consistent=true`、`approval_decisions_open_consistent=true` 和 `adapter_load_blocked_consistent=true` 只验证当前 no-store/no-load 约束一致，`approval_record_available=false`、`approval_review_queue_updated=false`、`approval_evidence_store_active=false`、`approval_decision_passed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `POST /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run`、Android Binder `dryRunHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalDecisionJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-decision-dry-run`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.decision.dry.run` 和 Linux gRPC/RPC `DryRunHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalDecision`，把 approval decision request 固定为 `HW-APD-001..008` contract-only 干跑入口。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_approval_decision_dry_run_active=true` 只表示请求形状可验证，`approval_decision_dry_run_state=rejected_blocked_contract_only`、`approval_authority_ready=false`、`approval_record_persisted=false`、`approval_decision_persisted=false`、`approval_review_queue_updated=false`、`approval_evidence_store_active=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/status`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalDecisionDryRunStatusJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-decision-dry-run-status`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.decision.dry.run.status` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalDecisionDryRunStatus`，把 approval decision dry-run 后的 no-store status 固定为 `HW-APS-001..008` contract-only 只读视图。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_approval_decision_dry_run_status_active=true` 只表示状态可查，`last_approval_decision_result_available=false`、`persisted_approval_decision_count=0`、`pending_approval_decision_review_count=0`、`approval_decision_review_queue_updated=false`、`approval_decision_evidence_store_active=false`、`decision_dry_run_post_called_by_status=false`、`approval_decision_persisted=false`、`approval_decision_passed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/audit-consistency`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalDecisionDryRunAuditConsistencyJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-decision-dry-run-audit-consistency`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.decision.dry.run.audit.consistency` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalDecisionDryRunAuditConsistency`，把 approval decision dry-run/status、approval authority audit/status 和 blocker rollup 的一致性固定为 `HW-APA-001..008` contract-only 只读视图。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_approval_decision_dry_run_audit_consistency_active=true` 只表示审计一致性可查，`consistency_passed=true` 只说明现有 contract surfaces 互相一致，`decision_dry_run_post_called_by_audit_consistency=false`、`approval_decision_persisted=false`、`approval_decision_review_queue_updated=false`、`approval_decision_evidence_store_active=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalDecisionClosureBlockerMatrixJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-decision-closure-blocker-matrix`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.decision.closure.blocker.matrix` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalDecisionClosureBlockerMatrix`，把 approval authority、approval policy、owner signature source、RBAC mapping、approval record schema、approval evidence store owner、review workflow、target smoke evidence、rollback plan、fault model、Driver/HAL gap closure evidence、audit owner 和 gate closure authority 固定为 `HW-APM-001..008` contract-only closure blocker matrix。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_approval_decision_closure_blocker_matrix_active=true` 只表示矩阵可查，`matrix_complete=true` 只说明开放依赖已结构化，`closure_ready=false`、`approval_decision_closure_allowed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalDecisionReviewerMatrixJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-decision-reviewer-matrix`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.decision.reviewer.matrix` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalDecisionReviewerMatrix`，把 approval authority、approval policy、signature/RBAC、approval record schema、approval evidence store、review workflow、target smoke、rollback/fault、Driver/HAL gap、audit export 和 gate closure reviewer 固定为 `HW-APR-001..008` contract-only reviewer matrix。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_approval_decision_reviewer_matrix_active=true` 只表示 reviewer matrix 可查，`unassigned_reviewer_count=11`、`review_ready=false`、`approval_review_allowed=false`、`retention_review_allowed=false`、`gate_closure_allowed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix/evidence-handoff-checklist`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffChecklistJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-reviewer-evidence-handoff-checklist`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.reviewer.evidence.handoff.checklist` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffChecklist`，把 reviewer identity、source blocker reference、evidence URI、owner signature、acceptance rule、retention policy、audit export 和 rollback/fault note 固定为 `HW-ARH-001..008` contract-only evidence handoff checklist。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_approval_reviewer_evidence_handoff_checklist_active=true` 只表示 handoff checklist 可查，`missing_handoff_packet_count=11`、`handoff_ready=false`、`evidence_handoff_allowed=false`、`approval_review_allowed=false`、`retention_review_allowed=false`、`gate_closure_allowed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix/evidence-handoff-checklist/acceptance-status`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceStatusJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-reviewer-evidence-handoff-acceptance-status`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.reviewer.evidence.handoff.acceptance.status` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceStatus`，把 reviewer handoff packet acceptance 固定为 `HW-AHA-001..008` contract-only acceptance status。该补充没有关闭 DEV-016：`owner_decision_evidence_adapter_load_approval_reviewer_evidence_handoff_acceptance_status_active=true` 只表示 acceptance status 可查，`blocked_acceptance_count=11`、`accepted_handoff_packet_count=0`、`acceptance_record_persisted_count=0`、`handoff_acceptance_allowed=false`、`approval_review_allowed=false`、`retention_review_allowed=false`、`gate_closure_allowed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix/evidence-handoff-checklist/acceptance-status/audit-consistency`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceAuditConsistencyJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-reviewer-evidence-handoff-acceptance-audit-consistency`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.reviewer.evidence.handoff.acceptance.audit.consistency` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceAuditConsistency`，把 reviewer handoff acceptance audit 固定为 `HW-AHC-001..008` contract-only audit consistency。该补充没有关闭 DEV-016：`approval_reviewer_evidence_handoff_acceptance_audit_consistency_active=true` 和 `consistency_passed=true` 只表示 checklist/status/parity/no-side-effect 计数互相一致，`blocked_acceptance_count=11`、`accepted_handoff_packet_count=0`、`acceptance_record_persisted_count=0`、`handoff_acceptance_allowed=false`、`approval_review_allowed=false`、`retention_review_allowed=false`、`gate_closure_allowed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix/evidence-handoff-checklist/acceptance-status/decision-rollup`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceDecisionRollupJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-reviewer-evidence-handoff-acceptance-decision-rollup`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.reviewer.evidence.handoff.acceptance.decision.rollup` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceDecisionRollup`，把 reviewer handoff acceptance 的 acceptance authority、acceptance record store、review workflow、audit retention、rollback/fault、Driver/HAL acceptance reviewer、gate closure authority 与 handoff packet presence 固定为 `HW-AHD-001..008` contract-only decision rollup。该补充没有关闭 DEV-016：`approval_reviewer_evidence_handoff_acceptance_decision_rollup_active=true` 只表示汇总可查，`decision_rollup_complete=true` 和 `decision_rollup_consistent=true` 只说明当前 contract surface 一致，`required_decision_count=8`、`blocked_decision_count=8`、`acceptance_decision_ready=false`、`accepted_handoff_packet_count=0`、`acceptance_record_persisted_count=0`、`handoff_acceptance_allowed=false`、`approval_review_allowed=false`、`retention_review_allowed=false`、`gate_closure_allowed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix/evidence-handoff-checklist/acceptance-status/decision-rollup/closure-readiness-checklist`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessChecklistJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-reviewer-evidence-handoff-acceptance-closure-readiness-checklist`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.reviewer.evidence.handoff.acceptance.closure.readiness.checklist` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessChecklist`，把 reviewer handoff acceptance closure readiness 的 acceptance authority、record store、review workflow、audit retention、rollback/fault、Driver/HAL acceptance、gate closure authority 与 handoff packet presence 固定为 `HW-AHE-001..008` contract-only checklist。该补充没有关闭 DEV-016：`approval_reviewer_evidence_handoff_acceptance_closure_readiness_checklist_active=true` 只表示 closure readiness 可查，`closure_readiness_complete=true` 只说明当前 contract surfaces 已绑定，`closure_ready=false`、`closure_blocker_count=8`、`ready_closure_check_count=0`、`accepted_handoff_packet_count=0`、`acceptance_record_persisted_count=0`、`handoff_acceptance_allowed=false`、`approval_review_allowed=false`、`retention_review_allowed=false`、`gate_closure_allowed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix/evidence-handoff-checklist/acceptance-status/decision-rollup/closure-readiness-checklist/audit-consistency`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessAuditConsistencyJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-reviewer-evidence-handoff-acceptance-closure-readiness-audit-consistency`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.reviewer.evidence.handoff.acceptance.closure.readiness.audit.consistency` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessAuditConsistency`，把 reviewer handoff acceptance closure readiness audit 固定为 `HW-AHF-001..008` contract-only consistency check。该补充没有关闭 DEV-016：`approval_reviewer_evidence_handoff_acceptance_closure_readiness_audit_consistency_active=true` 和 `consistency_passed=true` 只表示 checklist/count/blocker/rollup/parity/no-side-effect 计数一致，`closure_ready=false`、`closure_blocker_count=8`、`ready_closure_check_count=0`、`accepted_handoff_packet_count=0`、`acceptance_record_persisted_count=0`、`handoff_acceptance_allowed=false`、`approval_review_allowed=false`、`retention_review_allowed=false`、`gate_closure_allowed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-08 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix/evidence-handoff-checklist/acceptance-status/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessDecisionRollupJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-reviewer-evidence-handoff-acceptance-closure-readiness-decision-rollup`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.reviewer.evidence.handoff.acceptance.closure.readiness.decision.rollup` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessDecisionRollup`，把 reviewer handoff acceptance closure readiness decision 固定为 `HW-AHG-001..008` contract-only decision rollup。该补充没有关闭 DEV-016：`approval_reviewer_evidence_handoff_acceptance_closure_readiness_decision_rollup_active=true`、`decision_rollup_complete=true` 和 `decision_rollup_consistent=true` 只表示当前 contract surfaces 的阻塞决策可查且一致，`closure_ready=false`、`closure_decision_ready=false`、`blocked_decision_count=8`、`closure_blocker_count=8`、`accepted_handoff_packet_count=0`、`acceptance_record_persisted_count=0`、`handoff_acceptance_allowed=false`、`approval_review_allowed=false`、`retention_review_allowed=false`、`gate_closure_allowed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix/evidence-handoff-checklist/acceptance-status/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup/reviewer-assignment-checklist`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessDecisionReviewerAssignmentChecklistJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-reviewer-evidence-handoff-acceptance-closure-readiness-decision-reviewer-assignment-checklist`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.reviewer.evidence.handoff.acceptance.closure.readiness.decision.reviewer.assignment.checklist` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessDecisionReviewerAssignmentChecklist`，把 closure decision reviewer assignment 固定为 `HW-AHH-001..008` contract-only checklist。该补充没有关闭 DEV-016：`reviewer_assignment_checklist_complete=true` 只表示未分配 reviewer 项可查，`reviewer_assignment_ready=false`、`assigned_reviewer_count=0`、`unassigned_reviewer_count=8`、`reviewer_assignments_persisted=false`、`reviewer_assignment_queue_updated=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix/evidence-handoff-checklist/acceptance-status/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup/reviewer-assignment-checklist/audit-consistency`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessDecisionReviewerAssignmentAuditConsistencyJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-reviewer-evidence-handoff-acceptance-closure-readiness-decision-reviewer-assignment-audit-consistency`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.reviewer.evidence.handoff.acceptance.closure.readiness.decision.reviewer.assignment.audit.consistency` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessDecisionReviewerAssignmentAuditConsistency`，把 closure decision reviewer assignment audit 固定为 `HW-AHI-001..008` contract-only consistency view。该补充没有关闭 DEV-016：`consistency_passed=true` 只表示 reviewer assignment checklist/count/blocker/parity/no-side-effect 计数一致，`reviewer_assignment_ready=false`、`assigned_reviewer_count=0`、`unassigned_reviewer_count=8`、`reviewer_assignments_persisted=false`、`reviewer_assignment_queue_updated=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix/evidence-handoff-checklist/acceptance-status/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup/reviewer-assignment-checklist/audit-consistency/decision-rollup`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollupJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-reviewer-evidence-handoff-acceptance-closure-readiness-decision-reviewer-assignment-audit-decision-rollup`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.reviewer.evidence.handoff.acceptance.closure.readiness.decision.reviewer.assignment.audit.decision.rollup` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollup`，把 closure decision reviewer assignment audit decision 固定为 `HW-AHJ-001..008` contract-only decision rollup。该补充没有关闭 DEV-016：`decision_rollup_complete=true` 和 `decision_rollup_consistent=true` 只表示当前 reviewer assignment audit surface 的阻塞决策可查且一致，`reviewer_assignment_decision_blocked=true`、`adapter_load_decision=blocked-by-unassigned-reviewers`、`required_decision_count=8`、`blocked_decision_count=8`、`assigned_reviewer_count=0`、`unassigned_reviewer_count=8`、`reviewer_assignments_persisted=false`、`reviewer_assignment_queue_updated=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /hardware/interfaces/owner-decision-evidence/adapter-load-approval-authority-checklist/decision-dry-run/closure-blocker-matrix/reviewer-matrix/evidence-handoff-checklist/acceptance-status/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup/reviewer-assignment-checklist/audit-consistency/decision-rollup/closure-handoff-readiness-summary`、Android Binder `getHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollupClosureHandoffReadinessSummaryJson`、Linux CLI `hardware-interface-owner-decision-evidence-adapter-load-approval-reviewer-evidence-handoff-acceptance-closure-readiness-decision-reviewer-assignment-audit-decision-rollup-closure-handoff-readiness-summary`、Linux IPC `hardware.interfaces.owner.decision.evidence.adapter.load.approval.reviewer.evidence.handoff.acceptance.closure.readiness.decision.reviewer.assignment.audit.decision.rollup.closure.handoff.readiness.summary` 和 Linux gRPC/RPC `GetHardwareInterfaceOwnerDecisionEvidenceAdapterLoadApprovalReviewerEvidenceHandoffAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollupClosureHandoffReadinessSummary`，把 adapter-load closure handoff readiness 固定为 `HW-AHK-001..008` contract-only summary。该补充没有关闭 DEV-016：`closure_handoff_readiness_complete=true` 只表示当前八个 handoff dependency 被列明，`closure_handoff_ready=false`、`handoff_ready=false`、`open_handoff_dependency_count=8`、`reviewer_assignment_decision_blocked=true`、`adapter_load_decision=blocked-by-unassigned-reviewers`、`assigned_reviewer_count=0`、`unassigned_reviewer_count=8`、`reviewer_assignments_persisted=false`、`reviewer_assignment_queue_updated=false`、`handoff_acceptance_allowed=false`、`gate_closure_allowed=false`、`adapter_load_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-blocker-rollup`、Android Binder `getEventSubscriptionActivationApprovalDecisionBlockerRollupJson`、Android Console `Sub ApBlock`、Linux CLI `event-subscription-activation-approval-decision-blocker-rollup`、Linux IPC `uib.events.subscriptions.activation.approval.decision.blocker.rollup` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionBlockerRollup`，把 Event subscription activation approval decision 的阻塞原因固定为 `EV-ADB-001..008` contract-only rollup。该补充没有关闭 DEV-007：`approval_decision_blocker_rollup_active=true` 只表示阻塞项可查，`open_blocker_count=7`、`approval_decision_ready=false`、`approval_dry_run_allowed=false`、`approval_result_store_created=false`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`broker_active=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `POST /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run`、Android Binder `dryRunEventSubscriptionActivationApprovalDecisionJson`、Android Console `Sub ApDec`、Linux CLI `event-subscription-activation-approval-decision-dry-run`、Linux IPC `uib.events.subscriptions.activation.approval.decision.dry.run` 和 Linux gRPC/RPC `DryRunEventSubscriptionActivationApprovalDecision`，把 Event subscription activation approval decision dry-run 请求形状固定为 `EV-ADD-001..008` contract-only dry-run。该补充没有关闭 DEV-007：`approval_decision_dry_run_active=true` 只表示 dry-run request contract 可校验，`rejected_blocked_contract_only=true`、`approval_decision_ready=false`、`approval_dry_run_allowed=false`、`dry_run_request_persisted=false`、`dry_run_result_persisted=false`、`approval_decision_persisted=false`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/status`、Android Binder `getEventSubscriptionActivationApprovalDecisionDryRunStatusJson`、Android Console `Sub ApDStat`、Linux CLI `event-subscription-activation-approval-decision-dry-run-status`、Linux IPC `uib.events.subscriptions.activation.approval.decision.dry.run.status` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionDryRunStatus`，把 Event subscription activation approval decision dry-run 后的 no-store status 固定为 `EV-ADS-001..008` contract-only 只读视图。该补充没有关闭 DEV-007：`activation_approval_decision_dry_run_status_active=true` 只表示 status 可查，`last_approval_decision_result_available=false`、`persisted_dry_run_request_count=0`、`persisted_dry_run_result_count=0`、`persisted_approval_decision_count=0`、`decision_dry_run_post_called_by_status=false`、`approval_result_store_created=false`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/audit-consistency`、Android Binder `getEventSubscriptionActivationApprovalDecisionDryRunAuditConsistencyJson`、Android Console `Sub ApDAudit`、Linux CLI `event-subscription-activation-approval-decision-dry-run-audit-consistency`、Linux IPC `uib.events.subscriptions.activation.approval.decision.dry.run.audit.consistency` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionDryRunAuditConsistency`，把 Event subscription activation approval decision dry-run audit consistency 固定为 `EV-ADA-001..008` contract-only 只读视图。该补充没有关闭 DEV-007：`activation_approval_decision_dry_run_audit_consistency_active=true` 和 `consistency_passed=true` 只表示 blocker rollup、request contract、no-store status、Android/Linux parity 和 no-side-effect 计数互相一致，`decision_dry_run_post_called_by_audit_consistency=false`、`persisted_dry_run_request_count=0`、`persisted_dry_run_result_count=0`、`persisted_approval_decision_count=0`、`open_blocker_count=7`、`approval_result_store_created=false`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix`、Android Binder `getEventSubscriptionActivationApprovalDecisionClosureBlockerMatrixJson`、Android Console `Sub ApClose`、Linux CLI `event-subscription-activation-approval-decision-closure-blocker-matrix`、Linux IPC `uib.events.subscriptions.activation.approval.decision.closure.blocker.matrix` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionClosureBlockerMatrix`，把 Event subscription activation approval decision closure blockers 固定为 `EV-ACB-001..010` contract-only matrix。该补充没有关闭 DEV-007：`activation_approval_decision_closure_blocker_matrix_active=true` 只表示 closure blockers 可查，`closure_ready=false`、`open_closure_blocker_count=10`、`decision_dry_run_post_called_by_closure_blocker_matrix=false`、`persisted_dry_run_request_count=0`、`persisted_dry_run_result_count=0`、`persisted_approval_decision_count=0`、`approval_result_store_created=false`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`broker_active=false`、`high_rate_data_plane_active=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffChecklistJson`、Android Console `Sub ApHand`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-checklist`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.checklist` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffChecklist`，把 Event subscription activation approval decision owner handoff 槽位固定为 `EV-ACH-001..010` contract-only checklist。该补充没有关闭 DEV-007：`activation_approval_decision_owner_handoff_checklist_active=true` 只表示 owner handoff slots 可查，`owner_handoff_ready=false`、`open_owner_handoff_count=10`、`assigned_owner_count=0`、`unassigned_owner_count=10`、`owner_assignments_persisted=false`、`owner_handoff_queue_updated=false`、`decision_dry_run_post_called_by_owner_handoff_checklist=false`、`persisted_dry_run_request_count=0`、`persisted_dry_run_result_count=0`、`persisted_approval_decision_count=0`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`broker_active=false`、`high_rate_data_plane_active=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffAuditConsistencyJson`、Android Console `Sub ApHAud`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-audit-consistency`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.audit.consistency` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffAuditConsistency`，把 Event subscription activation approval decision owner handoff audit 固定为 `EV-AHA-001..010` contract-only consistency view。该补充没有关闭 DEV-007：`activation_approval_decision_owner_handoff_audit_consistency_active=true` 和 `consistency_passed=true` 只表示 handoff checklist、closure blocker matrix、count parity、source binding、no-store、Android/Linux parity 和 no-side-effect 计数一致，`owner_handoff_ready=false`、`assigned_owner_count=0`、`unassigned_owner_count=10`、`attached_evidence_count=0`、`owner_assignments_persisted=false`、`owner_handoff_queue_updated=false`、`decision_dry_run_post_called_by_owner_handoff_audit_consistency=false`、`persisted_dry_run_request_count=0`、`persisted_dry_run_result_count=0`、`persisted_approval_decision_count=0`、`pending_approval_decision_review_count=0`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`broker_active=false`、`high_rate_data_plane_active=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffDecisionRollupJson`、Android Console `Sub ApHRoll`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-decision-rollup`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.decision.rollup` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffDecisionRollup`，把 Event subscription activation approval decision owner handoff decision 固定为 `EV-AHD-001..010` contract-only decision rollup。该补充没有关闭 DEV-007：`activation_approval_decision_owner_handoff_decision_rollup_active=true`、`decision_rollup_complete=true` 和 `decision_rollup_consistent=true` 只表示当前 owner handoff audit/checklist 的阻塞决策可查且一致，`owner_handoff_decision_blocked=true`、`approval_decision_ready=false`、`approval_dry_run_allowed=false`、`required_decision_count=10`、`blocked_decision_count=10`、`assigned_owner_count=0`、`unassigned_owner_count=10`、`attached_evidence_count=0`、`approval_result_store_created=false`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`broker_active=false`、`high_rate_data_plane_active=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceReadinessMatrixJson`、Android Console `Sub ApHEv`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-readiness-matrix`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.readiness.matrix` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceReadinessMatrix`，把 Event subscription activation approval decision owner handoff evidence readiness 固定为 `EV-AHE-001..010` contract-only matrix。该补充没有关闭 DEV-007：`activation_approval_decision_owner_handoff_evidence_readiness_matrix_active=true`、`readiness_matrix_complete=true` 和 `readiness_matrix_consistent=true` 只表示 missing evidence packet 状态可查且一致，`handoff_evidence_ready=false`、`approval_decision_ready=false`、`required_evidence_packet_count=10`、`missing_evidence_packet_count=10`、`attached_evidence_count=0`、`persisted_evidence_packet_count=0`、`evidence_uri_count=0`、`owner_signature_count=0`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`broker_active=false`、`high_rate_data_plane_active=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceReadinessAuditConsistencyJson`、Android Console `Sub ApHEvAud`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-readiness-audit-consistency`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.readiness.audit.consistency` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceReadinessAuditConsistency`，把 Event subscription activation approval decision owner handoff evidence readiness audit 固定为 `EV-AHF-001..010` contract-only consistency view。该补充没有关闭 DEV-007：`activation_approval_decision_owner_handoff_evidence_readiness_audit_consistency_active=true` 和 `consistency_passed=true` 只表示 evidence packet count、source binding、Android/Linux parity、no-store/no-POST/no-side-effect 计数一致，`handoff_evidence_ready=false`、`approval_decision_ready=false`、`required_evidence_packet_count=10`、`missing_evidence_packet_count=10`、`attached_evidence_count=0`、`persisted_evidence_packet_count=0`、`evidence_uri_count=0`、`owner_signature_count=0`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`broker_active=false`、`high_rate_data_plane_active=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceStatusJson`、Android Console `Sub ApHAcc`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-status`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.status` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceStatus`，把 Event subscription activation approval decision owner handoff evidence acceptance 固定为 `EV-AHG-001..010` contract-only status view。该补充没有关闭 DEV-007：`activation_approval_decision_owner_handoff_evidence_acceptance_status_active=true` 和 `acceptance_status_consistent=true` 只表示 evidence packet acceptance 状态可查且与 readiness audit/matrix 一致，`handoff_evidence_acceptance_allowed=false`、`required_acceptance_count=10`、`blocked_acceptance_count=10`、`accepted_evidence_packet_count=0`、`acceptance_record_persisted_count=0`、`missing_evidence_packet_count=10`、`attached_evidence_count=0`、`persisted_evidence_packet_count=0`、`evidence_uri_count=0`、`owner_signature_count=0`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`broker_active=false`、`high_rate_data_plane_active=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status/audit-consistency`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceAuditConsistencyJson`、Android Console `Sub ApHAcAud`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-audit-consistency`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.audit.consistency` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceAuditConsistency`，把 Event subscription activation approval decision owner handoff evidence acceptance audit 固定为 `EV-AHH-001..010` contract-only consistency view。该补充没有关闭 DEV-007：`activation_approval_decision_owner_handoff_evidence_acceptance_audit_consistency_active=true` 和 `consistency_passed=true` 只表示 acceptance status、readiness audit、Android/Linux parity、no-store、no-POST 和 no-side-effect 计数一致，`handoff_evidence_acceptance_allowed=false`、`required_audit_count=10`、`blocked_acceptance_count=10`、`accepted_evidence_packet_count=0`、`acceptance_record_persisted_count=0`、`missing_evidence_packet_count=10`、`attached_evidence_count=0`、`persisted_evidence_packet_count=0`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`broker_active=false`、`high_rate_data_plane_active=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status/audit-consistency/decision-rollup`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceDecisionRollupJson`、Android Console `Sub ApHDec`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-decision-rollup`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.decision.rollup` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceDecisionRollup`，把 Event subscription activation approval decision owner handoff evidence acceptance decision 固定为 `EV-AHI-001..010` contract-only blocked rollup。该补充没有关闭 DEV-007：`decision_rollup_complete=true` 和 `decision_rollup_consistent=true` 只表示 acceptance status、audit consistency、readiness matrix、Android/Linux parity、no-store、no-POST 和 no-side-effect 计数一致，`acceptance_decision_ready=false`、`handoff_evidence_acceptance_allowed=false`、`approval_review_allowed=false`、`gate_closure_allowed=false`、`broker_activation_allowed=false`、`accepted_evidence_packet_count=0`、`acceptance_record_persisted_count=0`、`missing_evidence_packet_count=10`、`review_queue_updated=false`、`gates_closed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status/audit-consistency/decision-rollup/closure-readiness-checklist`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessChecklistJson`、Android Console `Sub ApHClose`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-closure-readiness-checklist`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.closure.readiness.checklist` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessChecklist`，把 Event subscription activation approval decision owner handoff evidence acceptance closure readiness 固定为 `EV-AHJ-001..010` contract-only checklist。该补充没有关闭 DEV-007：`closure_readiness_checklist_complete=true` 和 `closure_readiness_consistent=true` 只表示当前 closure blockers 与 EV-AHI/EV-AHH/EV-AHG/EV-AHE source surfaces 一致，`closure_ready=false`、`open_closure_check_count=10`、`blocked_decision_count=10`、`blocked_acceptance_count=10`、`accepted_evidence_packet_count=0`、`acceptance_record_persisted_count=0`、`missing_evidence_packet_count=10`、`approval_review_allowed=false`、`gate_closure_allowed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`review_queue_updated=false`、`gates_closed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status/audit-consistency/decision-rollup/closure-readiness-checklist/audit-consistency`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessAuditConsistencyJson`、Android Console `Sub ApHClAud`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-closure-readiness-audit-consistency`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.closure.readiness.audit.consistency` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessAuditConsistency`，把 Event subscription activation approval decision owner handoff evidence acceptance closure readiness audit 固定为 `EV-AHK-001..010` contract-only consistency view。该补充没有关闭 DEV-007：`activation_approval_decision_owner_handoff_evidence_acceptance_closure_readiness_audit_consistency_active=true` 和 `consistency_passed=true` 只表示 EV-AHJ/EV-AHI/EV-AHH/EV-AHG/EV-AHE 的 source binding、closure blocker、Android/Linux parity、no-store、no-POST 和 no-side-effect 计数一致，`closure_ready=false`、`open_closure_check_count=10`、`blocked_decision_count=10`、`blocked_acceptance_count=10`、`accepted_evidence_packet_count=0`、`acceptance_record_persisted_count=0`、`missing_evidence_packet_count=10`、`approval_review_allowed=false`、`gate_closure_allowed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`review_queue_updated=false`、`gates_closed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-09 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status/audit-consistency/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionRollupJson`、Android Console `Sub ApHClR`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-closure-readiness-decision-rollup`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.closure.readiness.decision.rollup` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionRollup`，把 Event subscription activation approval decision owner handoff evidence acceptance closure readiness decision 固定为 `EV-AHL-001..010` contract-only decision rollup。该补充没有关闭 DEV-007：`activation_approval_decision_owner_handoff_evidence_acceptance_closure_readiness_decision_rollup_active=true`、`decision_rollup_complete=true`、`decision_rollup_consistent=true` 和 `closure_readiness_audit_consistent=true` 只表示 closure readiness audit、closure blocker、acceptance decision、Android/Linux parity、no-store、no-POST 和 no-side-effect 决策状态一致，`closure_ready=false`、`closure_decision_ready=false`、`required_decision_count=10`、`blocked_decision_count=10`、`accepted_evidence_packet_count=0`、`acceptance_record_persisted_count=0`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false`、`virtualization_development_triggered=false` 和 `service_dispatch_triggered=false` 仍是验收边界。

2026-07-10 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status/audit-consistency/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup/reviewer-assignment-checklist`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentChecklistJson`、Android Console `Sub ApHRev`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-closure-readiness-decision-reviewer-assignment-checklist`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.closure.readiness.decision.reviewer.assignment.checklist` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentChecklist`，把 Event subscription activation approval decision owner handoff evidence acceptance closure readiness decision reviewer assignment 固定为 `EV-AHM-001..010` contract-only checklist。该补充没有关闭 DEV-007：`reviewer_assignment_checklist_complete=true` 只表示 reviewer assignment gates 可查且与 EV-AHL source decision rollup 绑定，`reviewer_assignment_ready=false`、`assigned_reviewer_count=0`、`unassigned_reviewer_count=10`、`reviewer_assignments_persisted=false`、`reviewer_assignment_queue_updated=false`、`approval_review_allowed=false`、`gate_closure_allowed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`accepted_evidence_packet_count=0`、`acceptance_record_persisted_count=0`、`review_queue_updated=false`、`gates_closed=false`、`hardware_accessed=false`、`driver_development_triggered=false`、`virtualization_development_triggered=false` 和 `service_dispatch_triggered=false` 仍是验收边界。

2026-07-10 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status/audit-consistency/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup/reviewer-assignment-checklist/audit-consistency`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentAuditConsistencyJson`、Android Console `Sub ApHRvA`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-closure-readiness-decision-reviewer-assignment-audit-consistency`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.closure.readiness.decision.reviewer.assignment.audit.consistency` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentAuditConsistency`，把 Event subscription activation approval decision owner handoff evidence acceptance closure readiness decision reviewer assignment audit 固定为 `EV-AHN-001..010` contract-only consistency view。该补充没有关闭 DEV-007：`consistency_passed=true` 只表示 reviewer assignment checklist/count/blocker/no-store/no-POST/no-queue-gate-broker、Android/Linux parity 和 no-side-effect 计数一致，`reviewer_assignment_ready=false`、`assigned_reviewer_count=0`、`unassigned_reviewer_count=10`、`reviewer_assignments_persisted=false`、`reviewer_assignment_queue_updated=false`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false`、`virtualization_development_triggered=false` 和 `service_dispatch_triggered=false` 仍是验收边界。

2026-07-10 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status/audit-consistency/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup/reviewer-assignment-checklist/audit-consistency/decision-rollup`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollupJson`、Android Console `Sub ApHRvRoll`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-closure-readiness-decision-reviewer-assignment-audit-decision-rollup`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.closure.readiness.decision.reviewer.assignment.audit.decision.rollup` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollup`，把 Event subscription activation approval decision owner handoff evidence acceptance closure readiness decision reviewer assignment audit decision 固定为 `EV-AHO-001..010` contract-only blocked rollup。该补充没有关闭 DEV-007：`decision_rollup_complete=true` 和 `decision_rollup_consistent=true` 只表示 reviewer assignment audit、unassigned reviewer count、blocked decision、no-store、no-POST 和 no-side-effect 决策一致，`reviewer_assignment_decision_blocked=true`、`reviewer_assignment_decision_ready=false`、`reviewer_assignment_decision=blocked-by-unassigned-reviewers`、`assigned_reviewer_count=0`、`unassigned_reviewer_count=10`、`reviewer_assignments_persisted=false`、`reviewer_assignment_queue_updated=false`、`review_queue_updated=false`、`gates_closed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-10 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status/audit-consistency/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup/reviewer-assignment-checklist/audit-consistency/decision-rollup/closure-handoff-readiness-summary`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollupClosureHandoffReadinessSummaryJson`、Android Console `Sub ApHReady`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-closure-readiness-decision-reviewer-assignment-audit-decision-rollup-closure-handoff-readiness-summary`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.closure.readiness.decision.reviewer.assignment.audit.decision.rollup.closure.handoff.readiness.summary` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollupClosureHandoffReadinessSummary`，把 Event subscription activation approval decision owner handoff evidence acceptance closure handoff readiness 固定为 `EV-AHP-001..010` contract-only summary。该补充没有关闭 DEV-007：`closure_handoff_readiness_complete=true` 和 `decision_rollup_consistent=true` 只表示 EV-AHO blocked decision、handoff dependency summary、Android/Linux parity、no-store、no-POST 和 no-side-effect 一致，`closure_handoff_ready=false`、`handoff_ready=false`、`required_handoff_dependency_count=10`、`open_handoff_dependency_count=10`、`reviewer_assignment_decision_blocked=true`、`assigned_reviewer_count=0`、`unassigned_reviewer_count=10`、`reviewer_assignments_persisted=false`、`reviewer_assignment_queue_updated=false`、`review_queue_updated=false`、`gates_closed=false`、`hardware_accessed=false`、`driver_development_triggered=false` 和 `virtualization_development_triggered=false` 仍是验收边界。

2026-07-10 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status/audit-consistency/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup/reviewer-assignment-checklist/audit-consistency/decision-rollup/closure-handoff-readiness-summary/audit-consistency`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollupClosureHandoffReadinessAuditConsistencyJson`、Android Console `Sub ApHReadyA`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-closure-readiness-decision-reviewer-assignment-audit-decision-rollup-closure-handoff-readiness-audit-consistency`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.closure.readiness.decision.reviewer.assignment.audit.decision.rollup.closure.handoff.readiness.audit.consistency` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollupClosureHandoffReadinessAuditConsistency`，把 Event subscription activation approval decision owner handoff evidence acceptance closure handoff readiness audit 固定为 `EV-AHQ-001..010` contract-only consistency view。该补充没有关闭 DEV-007：`consistency_passed=true` 只表示 EV-AHP summary、handoff dependency count、blocked state、Android/Linux parity、no-store、no-POST、no-queue/gate/broker 和 no-side-effect 计数一致，`closure_handoff_ready=false`、`handoff_ready=false`、`required_handoff_dependency_count=10`、`open_handoff_dependency_count=10`、`reviewer_assignment_decision_blocked=true`、`assigned_reviewer_count=0`、`unassigned_reviewer_count=10`、`reviewer_assignments_persisted=false`、`reviewer_assignment_queue_updated=false`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false`、`virtualization_development_triggered=false` 和 `service_dispatch_triggered=false` 仍是验收边界。

2026-07-10 新增 `GET /uib/events/subscriptions/activation-evidence/approval-authority-checklist/decision-dry-run/closure-blocker-matrix/owner-handoff-checklist/audit-consistency/decision-rollup/handoff-evidence-readiness-matrix/audit-consistency/acceptance-status/audit-consistency/decision-rollup/closure-readiness-checklist/audit-consistency/decision-rollup/reviewer-assignment-checklist/audit-consistency/decision-rollup/closure-handoff-readiness-summary/audit-consistency/decision-rollup`、Android Binder `getEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollupClosureHandoffReadinessAuditDecisionRollupJson`、Android Console `Sub ApHReadyR`、Linux CLI `event-subscription-activation-approval-decision-owner-handoff-evidence-acceptance-closure-readiness-decision-reviewer-assignment-audit-decision-rollup-closure-handoff-readiness-audit-decision-rollup`、Linux IPC `uib.events.subscriptions.activation.approval.decision.owner.handoff.evidence.acceptance.closure.readiness.decision.reviewer.assignment.audit.decision.rollup.closure.handoff.readiness.audit.decision.rollup` 和 Linux gRPC/RPC `GetEventSubscriptionActivationApprovalDecisionOwnerHandoffEvidenceAcceptanceClosureReadinessDecisionReviewerAssignmentAuditDecisionRollupClosureHandoffReadinessAuditDecisionRollup`，把 Event subscription activation approval decision owner handoff evidence acceptance closure handoff readiness audit decision 固定为 `EV-AHR-001..010` contract-only blocked rollup。该补充没有关闭 DEV-007：`decision_rollup_complete=true` 和 `decision_rollup_consistent=true` 只表示 EV-AHQ audit consistency、blocked state、Android/Linux parity、no-store、no-POST、no-queue/gate/broker 和 no-side-effect 决策一致，`closure_handoff_audit_decision_ready=false`、`closure_handoff_audit_decision_blocked=true`、`closure_handoff_audit_decision=blocked-by-unassigned-reviewers`、`required_decision_count=10`、`blocked_decision_count=10`、`passed_audit_count=10`、`failed_audit_count=0`、`assigned_reviewer_count=0`、`unassigned_reviewer_count=10`、`reviewer_assignments_persisted=false`、`reviewer_assignment_queue_updated=false`、`review_queue_updated=false`、`gates_closed=false`、`broker_activation_allowed=false`、`activation_allowed=false`、`hardware_accessed=false`、`driver_development_triggered=false`、`virtualization_development_triggered=false` 和 `service_dispatch_triggered=false` 仍是验收边界。

## 新增偏差记录模板

| ID | 偏差 | 涉及需求 | 当前原因 | 风险 | 修正计划 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| DEV-XXX |  |  |  |  |  | Open |
